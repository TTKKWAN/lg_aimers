# 구조 개요 (High-Level)

```
                        open/data/*.csv  (대회 규칙이 고정하는 데이터 계약)
                                │
        ┌───────────────────────┴────────────────────────┐
        │                   pipeline.py                    │   실험 단계 공용 코어
        │   피처 생성 · EB 축소 · 타깃인코딩 · 모델 팩토리      │   (방법론을 바꿀 땐 여기만 수정)
        │   · 평가(BSS) · season 가중치                      │
        └──────┬───────────────────────────────┬───────────┘
               │ import                         │ import
   ┌───────────┴────────────┐       ┌───────────┴─────────────┐
   │   diag_*.py / run_*.py    │       │      build_final.py       │
   │  "이 차이가 진짜 신호인가, │──선택▶│  채택된 설정으로 전체 데이터 │
   │   노이즈인가?"를 측정      │       │  재학습 → 하나의 번들로 직렬화│
   └────────────────────────┘       └────────────┬─────────────┘
                                                    │ model/bundle.pkl
                                                    ▼
                                     ┌───────────────────────────┐
                                     │         script.py            │  제출 전용, 완전 독립 실행
                                     │  (pipeline.py의 피처 로직을    │  (zip 안에 pipeline.py를
                                     │   그대로 복제해 인라인으로 보유) │   넣을 수 없어 코드 복제)
                                     └────────────┬──────────────┘
                                                    ▼
                                          submit.zip → 리더보드
```

이 프로젝트의 코드는 두 개의 서로 다른 시간축 위에서 움직인다. 하나는 **탐색축**으로, `pipeline.py`가 정의하는 피처·모델·평가 로직을 `diag_*.py`(진단: 이 문제의 노이즈 바닥과 편향의 크기를 잰다)와 `run_*.py`(비교: 후보들을 같은 잣대로 겨룬다)가 계속 갈아 끼우며 실험한다. 다른 하나는 **제출축**으로, 그 시점까지 탐색축에서 "노이즈가 아니라 진짜 신호"라고 확인된 설정만 `build_final.py`가 전체 데이터로 재학습해 `model/bundle.pkl` 하나로 굳힌다. 두 축을 분리해 둔 이유는 단순하다 — 탐색은 계속 실패하고 갱신되어야 하지만, 제출물은 언제 리더보드에 올려도 그 순간 "탐색축이 도달한 최선"을 정확히 반영해야 하기 때문이다.

`script.py`가 `pipeline.py`를 import하지 않고 피처 생성 코드를 그대로 복제해 들고 있는 것은 중복처럼 보이지만 강제된 설계다. 제출 규격이 `model/`, `script.py`, `requirements.txt` 세 가지만 허용하기 때문에 `pipeline.py`를 zip에 동봉할 방법이 없고, 그래서 학습 때 쓴 피처 로직을 추론 코드 안에 얼려 넣어야 한다. 이 복제가 어긋나면(학습과 추론의 피처가 미묘하게 달라지면) 리더보드에서 조용히, 에러 없이 성능만 무너지는 가장 위험한 실패 모드가 되므로, `test_submission_path.py`가 매번 두 경로의 산출 피처가 값·순서까지 동일한지 직접 대조해 이 위험을 차단한다. 같은 이유로 `bundle.pkl`은 모델 가중치뿐 아니라 축소 통계량(prior)·타깃 인코딩 맵·재중심화 상수까지 추론에 필요한 모든 것을 자기 완결적으로 담아, 추론 시점에 학습 데이터를 다시 참조할 필요가 없게 한다.

무엇이 바뀌어도 되고 무엇이 고정인지는 `CLAUDE.md`가 규정한다. 대회 규칙·데이터 계약·제출 규격·리크 금지 조건은 절대 바뀌지 않는 축이고, 그 위에서 `pipeline.py`가 표현하는 피처 집합·모델 알고리즘·앙상블 구성·재중심화 여부는 언제든 교체 가능한 축이다. 지금까지의 실험이 보여준 것은, 이 두 번째 축을 고를 때 감(intuition)이나 단일 실행 점수를 믿지 말고 항상 "그 차이가 시드/폴드를 바꿔도 살아남는가"부터 확인해야 한다는 것 — 그래서 `run_*.py` 계열은 예외 없이 다중 시드·다중 폴드·paired 비교 구조를 강제하고, 이 평가 방법론 자체는 앞으로 어떤 모델 계열로 바뀌든 그대로 유지되는 상위 규약으로 둔다.
