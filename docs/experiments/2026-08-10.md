# 2026-08-10 실험 기록

- **투수별 pressure-response agent dossier — 전체과거 고정성향 기각**
  (`scripts/diag_pitcher_pressure_agent.py`; 예측
  `experiments/preds/pitcher_pressure_agent_allpast_{2022,2023,2024}.npz`):
  `pitcher_id`를 개인 lookup 키로 사용해 풀카운트, 3볼, 2스트라이크, RISP,
  high-LI, 접전, 뒤지는 상황, 후반접전, 2아웃 RISP, 만루에서의 투수별 반응을
  만들었다. 각 투수-시즌 평균을 제거한 상황 안/밖 잔차 차이에서 리그 공통 효과를
  뺀 뒤 effective sample size로 k=200/500 축소했다. 모든 학습/검증 행은 해당
  season보다 엄격히 과거인 시즌만 참조한다. 비교는 v8 current16
  HGB25/LGBM75와 fold별 `recenter_f=0.5`까지 포함했다.

  - core 5상황, k=200: **+13.46/-12.56/-14.69**, 평균 -4.60
  - extended 10상황, k=200: **+17.88/-91.34/-1.74**, 평균 -25.07
  - extended, k=500: **+11.38/-98.47/+13.90**, 평균 -24.40
  - extended k=200 + raw `pitcher_id` 모델입력 제거: **+34.92/-27.28/+24.36**,
    평균 +10.67, SE 9.27/9.10/8.43

  ID를 숫자 피처에서 빼고 dossier lookup으로만 쓴 후보가 가장 좋았지만 2023에서
  약 3SE의 유의한 손해가 나 채택하지 않는다. 이는 선수별 상황 반응이 한 시즌
  안에는 존재할 수 있어도 2019~과거 전체를 하나의 불변 '멘탈 성향'으로 합친 값은
  다음 시즌에 안정적으로 전이되지 않는다는 증거다. 기존 v8 production과 ZIP은
  변경하지 않았다.

- **pressure agent 직전 1시즌 refinement — 미채택** (동일 스크립트 `last1` 모드;
  예측 `experiments/preds/pitcher_pressure_agent_last1_*.npz`): 실행 환경의 SciPy
  import 지연은 BLAS/OpenMP 스레드를 1로 제한해 해소한 뒤 끝까지 재실행했다.
  직전 1시즌만 사용하고 k=500으로 강하게 축소하며 raw `pitcher_id`는 모델 입력에서
  제거한 extended 10상황 dossier의 v8 대비 recentered gain은
  **+6.59(SE 9.30)/-5.43(8.76)/+18.92(7.72)**, 평균 +6.69였다. 전체과거
  dossier보다 2023 손해가 크게 줄었고 최신 2024는 약 2.45SE 개선됐지만, 세 폴드
  방향이 일치하지 않고 2022/2023 효과가 노이즈 범위라 production에는 넣지 않는다.
  결론: ID lookup-only 개인 adapter 관점은 일부 가능성이 있으나 현재 고정
  pressure profile만으로는 v8을 교체할 일반화 근거가 부족하다.

- **논문 기반 동적 잠재상태(dynamic state) — 기각**
  (`scripts/diag_dynamic_state.py`; 예측
  `experiments/preds/dynamic_state_{2022,2023,2024}.npz`): 멀티시즌 야구의
  player-season random effect/AR(1) 관점에서 공통 과거 유지율, 선수별 시즌 간
  변화분산, 이전 시즌 표본오차를 strict-past endpoint로 추정했다. 현재 행의
  season-to-date 성공률은 관측분산에 따른 Kalman gain으로 결합해 prior mean,
  volatility, gain, posterior mean/SD, shock-z를 만들었다. 학습 행도 해당 season보다
  엄격히 과거인 시즌만 참조하며 검증 행끼리는 참조하지 않는다. v8 production
  HGB25/current16-LGBM75와 fold별 `recenter_f=0.5`까지 포함한 gain은 투수-only가
  **2022 -169.23(SE 13.88), 2023 -83.26(7.11), 2024 -24.04(10.08)**,
  투수+타자가 **-138.30(13.15)/-89.78(7.65)/-87.91(12.45)**였다. 현 시즌 EB와
  기존 GBDT가 필요한 업데이트를 이미 표현하고 있어 추가 상태 추정오차만 늘어난
  것으로 판단해 기각했으며 CatBoost와 결합하지 않았다.

- **CatBoost 선수×상황 categorical expert — 2시드 확인 후 v9 채택**
  (`scripts/diag_catboost_expert.py`, `scripts/diag_catboost_confirm.py`; 예측
  `experiments/preds/catboost_expert_*.npz`, `catboost_confirm_*.npz`): raw 숫자형
  pitcher/batter/team ID를 expert 수치 입력에서 제거하고 문자열 명목형 lookup key로
  사용했다. 손, count, base state, pressure state, inning bucket을 포함한 12개
  category와 최대 2차 조합을 CatBoost ordered target statistics로 학습했다.
  current-season 16개와 fixed-EB 수치 피처는 함께 사용했다. 단일 seed=2026에서
  CatBoost 30% 혼합의 제출형 gain은 **+45.58/+174.69/+36.88**로 모두 양수였다.
  seed=2718을 추가한 2시드 평균의 비중별 v8 대비 gain은 다음과 같다.

  - 30%: **+47.47(SE 4.32)/+164.17(4.68)/+37.76(4.34)**
  - 40%: **+57.93(5.75)/+213.17(6.24)/+44.99(5.79)**
  - 50%: **+65.72(7.19)/+259.30(7.79)/+49.55(7.23)**
  - 60%: **+70.84(8.63)/+302.57(9.34)/+51.44(8.68)**
  - 75%: **+73.49(10.78)/+362.11(11.67)/+49.26(10.84)**

  2024가 60% 이후 꺾이고 CatBoost-only 의존을 피하기 위해 **v8 40% + CatBoost
  60%**를 채택했다. 이전 ID target encoding -83점과 달리 선수 평균 하나를 중복
  추가하는 것이 아니라, ID를 서열 없는 category로 처리하고 상황 조합을 ordered
  축소하는 별도 모델 계열이라는 차이가 있다.

- **v9 production 빌드·제출 경로 검증 완료**
  (`scripts/build_catboost_final.py`, `scripts/test_catboost_submission_path.py`): 기존
  v8 HGB8/current-season LGBM3는 유지하고 전체 2019~2024로 CatBoost 2개(seed
  2026/2718, 350 trees, depth 7)를 학습했다. 혼합 2024 자연평균은 `0.48756376`,
  f=0.5 목표는 `0.47487122`, `logit_shift=-0.05136262`. 번들 43.6MB이며
  `requirements.txt`에 `catboost==1.2.8`을 추가했다. 기존 전체
  `scripts/test_submission_path.py`, 실제 v9 CatBoost feature/혼합 수치 직접 대조,
  source clean-room 및 ZIP 재해제 clean-room 5행 실행을 모두 통과했다. 245,789행
  모사 피처+추론은 **36.00초**다. ZIP 최상위는 정확히 `model/bundle.pkl`,
  `script.py`, `requirements.txt`이며 무결성 검사를 통과했다. 루트 `submit.zip`
  SHA-256은
  `8ea71f1aa8c27d94e81fcfb552d43f89c088ed018d64515755306d195561eeb9`다.
  교체 전 v8은 `backups/submit_v8_unsubmitted_backup.zip`에 보존했다. v9은
  **리더보드 미제출 후보**이며 확인 최고/안전판은 계속 v7 938 백업이다.

- **v9 리더보드 1000점 초과 확인 — 현재 최고 갱신**: 사용자가 루트
  `submit.zip`을 실제 제출해 1000점을 넘긴 것을 확인했다. 정확한 점수는 아직
  기록되지 않아 `1000+`로 표기한다. 마지막 확인 최고 v7의 938점보다 최소 63점
  높다. v8은 미제출이므로 실측 상승분을 family 재가중과 CatBoost 추가 사이에서
  직접 분해할 수는 없지만, forward 검증의 v8 직접 대비에서는 CatBoost 60% 혼합이
  +70.84/+302.57/+51.44였다. 확인 ZIP을
  `backups/submit_v9_1000plus_backup.zip`으로 byte-identical 보존했으며 SHA-256은
  `8ea71f1aa8c27d94e81fcfb552d43f89c088ed018d64515755306d195561eeb9`다.

- **v9 구조 진단 — 단일 strike 강조보다 조건부 expert 혼합 우선**: 확인 번들의
  CatBoost 2개 `PredictionValuesChange` 중요도를 평균했다. `game_type_cat`가
  15.81로 1위, `season`이 10.21로 2위였고, 원시/EB 투수 strike rate는 각각
  0.38(57위)/0.41(51위)였다. 중요도는 인과적 채택 근거가 아니므로 저장된 forward
  예측으로 v8 raw 대비 `v8 40% + CatBoost 60%`의 하위집단 BSS gain도 확인했다.
  game type R은 2022/2023/2024에 +72.4/+41.2/+58.6으로 안정적이었지만 F는
  +18.0/+2571.8/+1.6으로 시대 의존성이 매우 컸다. count별로는 대체로 양수였으나
  2022의 3-2가 -38.7, 2024의 1-0이 -4.4였다. 따라서 다음 우선순위는 특정 피처의
  임의 가중이 아니라, 완전 OOF로 만든 base/CatBoost 예측과 소수의 안정적 상태만
  사용하는 강한 정규화 조건부 mixture-of-experts다. 그룹별 숫자는 raw 진단이며
  production 재중심화를 포함한 채택 결과가 아니므로 현재 ZIP은 변경하지 않는다.

- **strict-past OOF 조건부 mixture-of-experts gate — 기각**
  (`scripts/diag_conditional_gate.py`, 로그 `logs/diag_conditional_gate.log`, 예측
  `experiments/preds/conditional_gate_oof_2021.npz`, 요약
  `experiments/preds/conditional_gate_summary.csv`): outer 2022도 자기 시즌을 보지
  않도록 production 동일 HGB8/current-season LGBM3/CatBoost2의 2021 OOF를 새로
  생성했다. 각 outer 시즌 gate는 2021부터 해당 시즌 직전까지의 OOF 행만 학습했다.
  `p_base + clip(Zβ,0,1)·(p_cat-p_base)` ridge gate에 global, game type, count
  전략상태, 정확 count, 현 시즌 workload reliability/cold-start를 단계적으로 넣고,
  후보별 마지막 학습 시즌 예측으로 production 동일 `recenter_f=0.5`를 다시 구했다.

  - global gate 최선: **-3.43/+74.32/-19.20**
  - coarse gate 최선 평균 후보(λ=1e-5): **-12.77/+154.95/-10.79**
  - exact-count gate 최선 평균 후보(λ=1e-4): **-10.09/+153.87/-12.44**
  - mechanism gate λ=1e-5: **-12.26(SE 3.21)/+155.23(4.39)/-11.52(3.23)**

  2023의 game-type regime 변화에서 얻은 큰 이득이 과거 OOF gate를 CatBoost 쪽으로
  과도하게 밀어 2024에 전이되지 않았다. 조건을 늘려도 2022/2024 두 폴드의 유의한
  손해가 사라지지 않으므로 고정 v8 40%/CatBoost 60%를 유지하고 gate는 기각한다.
  현재 production 번들과 ZIP은 변경하지 않았다.

- **현 시즌 command profile CatBoost expert — all4 50% 교체 채택 후보**
  (`scripts/diag_catboost_command_profile.py`,
  `scripts/diag_catboost_command_confirm.py`; 로그
  `logs/diag_catboost_command_profile.log`,
  `logs/diag_catboost_command_profile_2024.log`,
  `logs/diag_catboost_command_confirm.log`; 예측
  `experiments/preds/catboost_command_*`): 이전 시즌 endpoint를 fold-train에서
  고정하고 누적 asof에서 현 시즌 strike/ball/middle/reverse의 raw/EB/career-dev를
  각각 복원했다. workload/known/invalid는 기존 success 8개를 재사용하고 rate마다
  3개만 CatBoost에 추가했다. 1시드 스크린에서 strike-only는
  +1.18/-43.05로 조기 탈락했고 ball-only도 -2.55/-53.36이었다. 반면
  middle+reverse는 +25.27/+37.84/+4.08, all4는 +20.47/+22.29/+8.80으로 세 폴드
  모두 양수여서 두 번째 시드를 확인했다.

  두 시드 평균 command expert를 현재 CatBoost2와 교체한 최종 제출형 gain:

  - middle+reverse 50% 교체: **+16.82(SE 2.60)/+30.12(2.21)/+5.27(2.18)**
  - middle+reverse 100% 교체: **+25.01(5.20)/+54.41(4.41)/+4.42(4.37)**
  - all4 50% 교체: **+17.80(2.77)/+18.12(2.31)/+6.93(2.30)**
  - all4 100% 교체: **+25.80(5.55)/+29.86(4.62)/+7.09(4.60)**

  all4 100%는 평균 gain이 더 크지만 최신 폴드 이득이 50%와 사실상 같고 표준오차는
  두 배다. 안정성을 우선해 다음 단계 기준선은 **기존 CatBoost2 50% + all4
  command CatBoost2 50%**로 정한다. 전체 최종식은 여전히 v8 40% + CatBoost 계열
  60%이며, production ZIP 반영은 후속 전략과 함께 최종 결정한다.

- **count hierarchy 및 명시적 선수×전략 category — 조기 기각**
  (`scripts/diag_catboost_count_hierarchy.py`, 로그
  `logs/diag_catboost_count_hierarchy.log`, 예측
  `experiments/preds/catboost_count_hierarchy_{2022,2023}.npz`): stage2 all4-50
  기준선 위에서 exact count와 별도로 pitcher-ahead/even/batter-ahead,
  must-strike/chase-freedom/full/neutral, early/middle/deep 범주를 추가했다. 이어
  `pitcher_id×strategy`, `pitcher_id×advantage`, `batter_id×strategy`를 명시적
  문자열 category로 만들어 CatBoost의 ordered CTR에 넣었다.

  - hierarchy: **-3.45(SE 3.56)/-28.87(3.14)**
  - hierarchy + pitcher cross: **-8.25(3.89)/-1.18(3.22)**
  - hierarchy + pitcher/batter cross: **-0.61(3.95)/-15.67(3.33)**

  2022/2023 두 forward 폴드가 모두 비양수라 2024가 양수여도 세 폴드 방향
  일관성을 만족할 수 없다. 기존 exact `count_state_cat`과 CatBoost의 자동 2차
  category 조합이 이미 이 구조를 포착해 수동 hierarchy/cross가 중복 노이즈를
  더한 것으로 판단하고 최신 폴드 계산을 생략했다. stage2 all4-50 기준선을 유지한다.

- **CatBoost 피처 그룹 ablation — 제거 후보 없음**
  (`scripts/diag_catboost_ablation.py`, 로그 `logs/diag_catboost_ablation.log`,
  `logs/diag_catboost_ablation_2023.log`, 예측
  `experiments/preds/catboost_ablation_{2022,2023}.npz`): stage2 all4-50의 command
  expert 절반에서 원시 rate, 중복 경기상태, 달력, 팀 category, game type, season,
  win-context를 한 묶음씩 제거했다. 2022 gain은 각각
  **-0.77/-2.01/-10.60/-7.24/-39.50/-17.65/-4.59**였다. 1SE 이내로 남은 원시
  rate 제거와 중복 상태 제거를 2023에서 재검증했으나 각각
  **-6.71(SE 3.03)/-5.74(3.07)**로 유의하게 나빠졌다. 특히 중요도 1위였던
  `game_type_cat`과 `season`은 실제 ablation에서도 필수였다. 단순 feature pruning은
  채택하지 않는다.

- **game_type×season 구조 감사 및 유형별 재중심화 — 기각**
  (`scripts/diag_game_type_recenter.py`, 요약
  `experiments/preds/game_type_recenter_summary.csv`): stage2 all4-50 raw 예측을 F/R로
  나눠 마지막 학습 시즌 수준 또는 유형별 최근 3시즌 선형 외삽값 쪽으로 각각
  재중심화했다. 마지막 시즌 persistence f=0.25는
  **+35.25/-28.59/-48.92**, 유형별 선형 f=0.25는
  **+26.46/-390.87/-18.73**이었다. F 성공률이 2022 0.709에서 2023 0.473으로
  구조적으로 바뀌어 유형별 수준 추정이 다음 시즌에 전이되지 않는다. 현재 global
  `recenter_f=0.5`를 유지한다.

- **저랭크 pitcher×batter factorization expert — 기각**
  (`scripts/diag_factorization_expert.py`, 로그
  `logs/diag_factorization_expert.log`, 예측
  `experiments/preds/factorization_expert_{2022,2023,2024}.npz`, 요약
  `experiments/preds/factorization_expert_summary.csv`): fold-train에서 pitcher/batter
  fixed-EB 평균을 뺀 residual의 희소 pitcher×batter 행렬을 만들고, count shrinkage
  10/30/100과 truncated SVD rank 8/16/32, 두 seed를 비교했다. 관측 선수 쌍에는
  저랭크 내적을, unseen 선수에는 0 residual을 적용해 stage2 all4-50 기준선에
  5/10/20/30% 혼합했다. 10% 혼합의 대표 결과는 rank 8, shrink 10에서
  **-20.44(SE 3.75)/+137.49(4.01)/-13.84(2.97)**였고, 모든 rank/shrink 조합이
  2022와 2024에서 음수였다. 2023의 강한 이득은 다음 시즌에 전이되지 않으므로
  선수쌍 latent interaction은 기각하고 stage2 all4-50을 최종 production 후보로
  확정한다.

- **5전략 순차 평가 완료 및 v10 production 반영**
  (`scripts/build_catboost_command_final.py`): 조건부 OOF gate, 현 시즌 command,
  count hierarchy/선수×전략, ablation/game-type 감사, 저랭크 factorization을 순서대로
  평가했고 세 forward 폴드가 모두 양수인 all4 command 50%만 채택했다. 전체
  1,475,092행으로 command CatBoost seed 2026/2718을 재학습해 기존 CatBoost2와
  50:50, 다시 v8과 60:40으로 혼합했다. 2024 자연평균 `0.48751748`, f=0.5 목표
  `0.47484808`, `logit_shift=-0.05126671`, 번들 49.8MB다.

  `scripts/test_submission_path.py`의 command 12컬럼 학습↔제출 값·순서,
  batch/single·shuffle·unseen 검증, `scripts/test_catboost_submission_path.py`의 실제
  CatBoost family 혼합 수치 대조, source 및 최종 ZIP 재해제 clean-room 5행 실행을
  통과했다. 245,789행 모사 피처+추론은 102.64초다. ZIP 최상위는 정확히
  `model/bundle.pkl`, `script.py`, `requirements.txt`이며 SHA-256은
  `095ff3b7ce401c296600b7a9eb4c71f437d158d1cafb6fd6242add9c0fd89fb8`다. 현재 루트
  `submit.zip`은 **미제출 v10 후보**이고, 확인 최고 v9 1000+는
  `backups/submit_v9_1000plus_backup.zip`에 그대로 보존돼 있다.

- **v10 리더보드 1010점 확인 — 실질 개선 없음**: 사용자가 현재 루트
  `submit.zip`을 제출해 1010점을 확인했다. v9 1000+와 사실상 같은 결과로 판단해,
  현 시즌 all4 command CatBoost 50% 추가는 로컬 forward gain과 달리 리더보드에서
  유의미한 개선을 만들지 못한 것으로 기록한다. 모델과 ZIP은 변경하지 않았다.
