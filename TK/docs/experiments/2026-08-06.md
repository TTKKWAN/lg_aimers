# 2026-08-06 실험 기록

- **2026-08-06 v1 baseline** (RandomForest, organizer 제공 하이퍼파라미터):
  2024 홀드아웃 brier=0.248767, **Score=416.18**. 로그: `logs/train_baseline.log`.
- **2026-08-06 HGB 하이퍼파라미터 탐색 10종** (`scripts/experiment_search{,2,3}.py`,
  로그 동일 접두 `logs/*.log`): learning_rate/max_leaf_nodes/min_samples_leaf/
  l2_regularization 조합 + pitcher_id·batter_id 포함/제외 ablation. 결론 —
  (a) ID를 빼면 오히려 악화(395.74 < 408.54) → `asof_*` 집계만으로는 개별
  선수 고유 신호를 다 못 담음, ID 자체가 완전한 노이즈는 아님. (b) learning_rate를
  낮추고 min_samples_leaf를 줄이는 방향이 유효했지만 특정 지점(lr=.03,
  min_leaf=30) 이후로는 더 낮춰도 오히려 악화(과적합 시작) — 국소 최적점 확인.
- **2026-08-06 v2 채택** (HistGradientBoosting, config D + 파생 피처 10개): 2024
  홀드아웃 brier=0.248405, **Score=561.34** (v1 대비 +35% 상대개선), 245,789행
  추론 3.92초. 로그: `logs/train_final.log`. → 리더보드 실측 700점.
- **시도했지만 보류: `trackman_history.csv` 개체 단위 join** — main
  `pitcher_id`/`batter_id`와 trackman `pitcher_trackman_id`/`batter_trackman_id`
  간 겹치는 값이 0개라 조인 불가 확인. 팀 단위도 매핑이 불명확해 보류.
- **2026-08-06 시드 노이즈 진단** (`scripts/diag_seed_variance.py`): v2 config를
  시드 5개로만 바꿔 재학습 → 검증 score 417~561 (std=63, range=144). 두 모델
  점수차가 ~37점(2SE) 미만이면 통계적으로 구분 불가 판정. **v2에서 config를
  고른 근거 자체가 무효** — 이후 모든 비교를 다중 시드 앙상블 기준으로 전환.
  시드 5개 평균 앙상블만으로 +105점 (단일평균 496.5→601.5).
- **2026-08-06 시즌 base rate 드리프트 진단** (`scripts/diag_level.py`): 정답률이
  2019(.565)→2024(.486) 단조 하락. 과거→미래 예측 시 이 편향만으로 2023
  폴드는 -627점 손해(신호 전체를 삼켜 raw score가 0으로 클리핑). `season`
  피처는 반드시 유지해야 함(빼면 561→9.8로 폭락, `B_season_dropped` 참고) —
  모델이 `season` 값으로 부분적으로 자가보정하고 있었다는 뜻. 최근시즌만
  가중/학습하는 건 오히려 손해(샘플 부족이 드리프트 완화보다 큼).
- **2026-08-06 보정 기울기(calibration slope) 진단** (`scripts/diag_calibration.py`):
  `p'=r+a(p-r)`의 최적 a*가 폴드마다 전혀 다름(2023: a*=0.157, 2024: a*=0.883).
  2023의 a를 2024에 적용하면 654.94→216.10으로 폭락 → **α 보정은 시즌 간
  전이 안 됨, 미채택**. 반면 정답률 자체를 추세 외삽으로 재중심화하는 건
  2024 폴드에서 oracle(실제 r을 앎)과 거의 같은 성능(675.18 vs 676.30) —
  외삽이 실제로 정확함을 확인, v3 재중심화의 근거.
- **2026-08-06 v3 최종 후보 비교** (`scripts/run_final_search.py`, fold=2024,
  이질 앙상블 8개): `shrink_k50`(EB축소만) raw=608.73/외삽재중심화=678.22로
  최고. `shrink_k200`은 통계적으로 동일(-7.22점, SE=9.9, <1σ) → k값 자체는
  둔감. `shrink_k50_te`(+타깃인코딩)는 **-83.05점**(SE=10.1, ≈8σ, 확실한 손해)
  → 타깃인코딩 기각. **v3 채택: EB축소(k=50) + 이질앙상블8 + 재중심화(f=0.5)**.
- **2026-08-06 제출 경로 검증** (`scripts/test_submission_path.py`): 학습
  파이프라인과 `script.py`가 만드는 91개 피처가 값·순서까지 완전히 동일함을
  소규모 번들로 직접 대조해 확인. `logit_shift`(재중심화 상수)가 정확히
  적용되는지도 검증. → v3 리더보드 실측 898점.
- **2026-08-06 `recenter_f` 스윕 진단** (`scripts/diag_recenter_f.py`, fold=2024,
  shrink_k50+이질앙상블8 고정): f=0(608.73) → f=0.5(656.96, 현재 제출본) →
  f=1.0(678.22) → f=1.25(678.74) → f=1.5(672.52, 하락 시작). f=1.0이 oracle
  (679.35, 실제 r_true 완전반영)에 거의 근접, f=0.5 대비 **+21.26점**
  (SE=4.6, ~4.6σ, 통계적으로 명확). 이 폴드는 외삽 r_hat(0.4877)이 실제
  r_true(0.4861)와 거의 일치(오차 0.0016)했던 케이스라 f를 밀어붙일수록
  이득이었음. f=1.0과 f=1.25는 통계적으로 구분 불가(SE=6.9) → f=1.0 채택,
  로그: `logs/diag_recenter_f.log`.
- **2026-08-06 v3 recenter_f=1.0 재빌드** (`python3 scripts/build_final.py 50
  te0 8 1.0`): 전체 2019~2024 재학습, 2025 외삽 정답률 0.4622 (최근 3시즌
  기울기 -0.0214/년), logit_shift=-0.10082 (기존 f=0.5는 -0.05035였음).
  로그: `logs/build_final.log`. clean-room 검증 통과 (`/tmp` 별도 디렉토리에서
  script.py 실행, 5행 샘플 → submission.csv 정상 생성). `submit.zip` 갱신
  (기존 f=0.5 버전은 `backups/submit_v3_f05_backup.zip`으로 백업).
- **2026-08-06 f=1.0 리더보드 실측 → 843점, f=0.5(898) 대비 -55점 손해로 확인
  (기각)**. 로컬 2024 폴드 스윕은 f=1.0이 f=0.5보다 +21점 우위라고 예측했지만
  (`diag_recenter_f.py`), 실제 2025 리더보드에서는 정반대 — **폴드의 외삽
  r_hat(0.4877)이 그 폴드 실제 r_true(0.4861)와 우연히 근접했던 것뿐이고, 그
  신뢰도가 2025 실측까지 전이되지 않았다**. 이전에 확인한 calibration slope
  α*의 폴드 간 비전이(2023 vs 2024, α*=0.157 vs 0.883)와 같은 패턴 — 재중심화
  강도 자체가 시즌마다 흔들리는 불안정한 양이라는 두 번째 증거. **`submit.zip`
  과 `open/baseline_submit/model/bundle.pkl`을 f=0.5 버전으로 되돌림**(현재
  최고 기록 898점). f=1.0 번들은 `bundle_f1_backup.pkl`로 참고용 보관만.
  **교훈**: recenter_f는 단일 폴드(2024)만으로 상향 조정하면 안 됨 — 여러
  폴드(2022→2023, 2023→2024 등)에서 일관되게 이득이 나는지 확인하거나, 아예
  다음 시도에서는 f를 낮추는 방향(0.25~0.4권 재탐색)도 고려할 것.

- **2026-08-06 trackman_history 시즌 집계 피처 진단** (`scripts/diag_trackman_features.py`,
  forward-chaining 폴드 2022/2023/2024, 5-멤버 이질앙상블, shrink_k50 baseline
  대비): 리그 시즌별 `rel_speed`/`spin_rate`/`induced_vert_break`/`horz_break`/
  `extension`의 평균·표준편차 + 구종군 비율(총 14개 피처, val_season 값은 이전
  시즌만으로 선형외삽) 추가 → **세 폴드 전부 손해**(2022: -20.85, 2023: -16.95,
  2024: -93.80, 폴드평균 -43.86, 각 SE 7~9로 통계적으로 유의). **기각**.
  개체(투수/타자) 단위 join이 막혀 있어 리그/시즌 집계만 가능했는데, 이 피처들이
  이미 있는 `season` 원본 피처(raw passthrough)와 정보가 사실상 중복되고,
  평가 시점(val_season) 값은 외삽이라 노이즈만 더한 것으로 추정. 특히 추세가
  뚜렷한 `induced_vert_break_mean`(2019 27.68→2024 22.80) 같은 컬럼의 외삽
  오차가 2024 폴드에서 가장 크게 손해를 낸 것과 일치. → **trackman_history는
  이 데이터 구조(익명화·개체 join 불가·2025 미포함)로는 활용 가치가 낮다는
  결론, 재시도 시 다른 접근(예: within-season 상대 순위처럼 외삽이 필요 없는
  형태) 필요**.
